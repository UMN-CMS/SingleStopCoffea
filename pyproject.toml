[tool.poetry]
name = "analyzer"
version = "0.1.0"
description = ""
authors = ["UMN Stealth Stop Group"]
license = "MIT"
readme = "README.org"

[tool.poetry.dependencies]
python = "^3.9"

coffea = {version="^2023", extras=["dask"]}
hist = "^2.6.3"
matplotlib = "^3.7"
jupyter = "*"

pyyaml = "^6.0"

#scipy = "^1.9"
#gpytorch  = ">=1.11"

[tool.poetry.group.dev.dependencies]
black = "^23.3.0"
mypy = "^1.3.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
