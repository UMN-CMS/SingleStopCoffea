PostDefaults:
  style_set: "style.yaml"
  plot_configuration:
    cms_text: "Preliminary"
    image_type: ".svg"
    cms_text_color: Black

Postprocessing:
  # - name: Cutflow
  #   action: PlotCutflow
  #   to_process:
  #     sample_name: '*'
  #     region_name: '*'
  #   grouping: 
  #     fields: ['dataset.era.name', 'region_name']
  #     axis_options: {variation: central}
  # 
  #   style_set:
  #     - style:
  #         plot_type: step
  # 
  #   output_name: "postprocessing/plots/cutflow_{region_name}_{histogram_name}"
  # 
  - name: Proof Of Concept Plots
    action: Histogram1D
    histogram_names: 
        # - dijet_pt
        # - dijet_delta_r
        # - dijet_pair_pt
        # - dijet_pair_delta_r
        - jet_pt
        - jet_eta
        - jet_phi
        - jet_ht
    to_process:
      region_name: Signal
    plot_configuration:
      cms_text: "Preliminary"
    stack:
        - tt_semilepton_2018
        - tt_had_2018
        - qcd_2018
    stackandplot: 
        - ggHHbbVV_2018
    scale: log

    grouping: 
      fields: ['dataset.era.name', 'region_name']
      axis_options: {variation: central}
    output_name: "postprocessing/plots/{region_name}_{histogram_name}"